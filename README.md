# psuseserverlessfwknodeaws_nt
```
serverless create --template aws-nodejs --path servicename
```

```
npm init && npm install 
```

options
```
sls deploy
sls deploy function --function Emailer
sls deploy --package package-path
```
